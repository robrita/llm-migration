# LLM Evaluation Tool Environment Variables
# 
# Model configuration (API keys, endpoints) is managed through the UI
# and stored locally in .conf/llm_eval_config.json (gitignored).
#
# For Azure Identity authentication (optional), ensure you're logged in:
#   az login
#
# Or set these for service principal authentication:
# AZURE_CLIENT_ID=your_client_id
# AZURE_TENANT_ID=your_tenant_id
# AZURE_CLIENT_SECRET=your_client_secret
